/*
pub enum BetSize {
    Minimum,
    Absolute(usize),
    RelativeToPot(f64),
    RelativeToBet(f64),
    AllIn,
}

pub struct Abstraction {
    bet_sizes: Vec<BetSize>,
    raise_sizes: Vec<BetSize>,
}

impl Abstraction {
    pub fn new() -> Self {
        Self {}
    }
}
*/
